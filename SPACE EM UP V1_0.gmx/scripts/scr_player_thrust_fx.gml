if !audio_is_playing(snd_thrust){
    if !gpConnected
        {
        if moveX != 0 || moveY!= 0 scr_sound(snd_thrust); 
        }
}
        if moveX = 0 && moveY = 0 audio_stop_sound(snd_thrust);
    else if gpConnected
        {
        if move scr_sound(snd_thrust,-1,0);
        else if !move audio_stop_sound(snd_thrust);
        }


